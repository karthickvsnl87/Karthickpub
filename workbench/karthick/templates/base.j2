hostname {{ hostname }}
ip domain name {{ domain_name }}
ip ssh version 2
{% if enable_netconf | default(true) %}
netconf-yang
{% endif %}
{% if enable_restconf | default(false) %}
restconf
{% endif %}
